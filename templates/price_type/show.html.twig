{% extends 'base.html.twig' %}

{% block title %}PriceType
{% endblock %}

{% block body %}
	<h1>PriceType</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ price_type.id }}</td>
			</tr>
			<tr>
				<th>Name</th>
				<td>{{ price_type.name }}</td>
			</tr>
			<tr>
				<th>Price</th>
				<td>{{ price_type.price }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_price_type_index') }}" class="btn btn-primary">back to list</a>

	<a href="{{ path('app_price_type_edit', {'id': price_type.id}) }}" class="btn btn-warning btn-sm">edit</a>

	{{ include('price_type/_delete_form.html.twig') }}
{% endblock %}
