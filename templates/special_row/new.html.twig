{# templates/special_row/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Create Special Row
{% endblock %}

{% block body %}
	<h1>Create new Special Row</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">Save</button>
	{{ form_end(form) }}

	<a href="{{ path('app_special_row_index') }}" class="btn btn-primary">back to list</a>
{% endblock %}
