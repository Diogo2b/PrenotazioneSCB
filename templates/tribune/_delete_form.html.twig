<form method="post" action="{{ path('app_tribune_delete', {'id': tribune.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tribune.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
