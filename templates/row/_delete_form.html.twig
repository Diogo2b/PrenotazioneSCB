<form method="post" action="{{ path('app_row_delete', {'id': row.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ row.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
