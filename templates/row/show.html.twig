{% extends 'base.html.twig' %}

{% block title %}Row
{% endblock %}

{% block body %}
	<h1>Row</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ row.id }}</td>
			</tr>
			<tr>
				<th>Sigle</th>
				<td>{{ row.sigle }}</td>
			</tr>
			<tr>
				<th>Capacity</th>
				<td>{{ row.capacity }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_row_index') }}" class="btn btn-primary">back to list</a>

	<a href="{{ path('app_row_edit', {'id': row.id}) }}" class="btn btn-warning">edit</a>

	{{ include('row/_delete_form.html.twig') }}
{% endblock %}
