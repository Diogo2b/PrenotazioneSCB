{% extends 'base.html.twig' %}

{% block title %}PaymentTicket
{% endblock %}

{% block body %}
	<h1>PaymentTicket</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ payment_ticket.id }}</td>
			</tr>
			<tr>
				<th>Payment</th>
				<td>{{ payment_ticket.payment.id }}</td>
			</tr>
			<tr>
				<th>Ticket</th>
				<td>{{ payment_ticket.ticket.id }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('app_payment_ticket_index') }}" class="btn btn-primary">Retour à la liste</a>

	<a href="{{ path('app_payment_ticket_edit', {'id': payment_ticket.id}) }}" class="btn btn-primary">Modifier</a>

	{{ include('payment_ticket/_delete_form.html.twig') }}
{% endblock %}
