<form method="post" action="{{ path('app_seat_delete', {'id': seat.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seat.id) }}">
	<button class="btn btn-danger">Supprimer</button>
</form>
