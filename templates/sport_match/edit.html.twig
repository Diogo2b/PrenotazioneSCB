{% extends 'base.html.twig' %}

{% block title %}Modifier le Match
{% endblock %}

{% block body %}
	<h1>Modifier le Match</h1>

	{{ form_start(form) }}
	{{ form_widget(form) }}
	<button class="btn btn-primary">Mettre à jour</button>
	{{ form_end(form) }}

	<a href="{{ path('app_sport_match_index') }}" class="btn btn-secondary mt-3">Retour à la liste</a>

	{{ include('sport_match/_delete_form.html.twig') }}
{% endblock %}
